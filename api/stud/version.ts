/**
 * Shared plugin version constants, used by both the plugin handshake and the
 * frontend connection diagnostics. Keep in sync with the PLUGIN_VERSION baked
 * into every generated plugin (api/stud/plugin.ts).
 */
export const PLUGIN_VERSION = "2.0.0";
export const MIN_PLUGIN_VERSION = "2.0.0";
